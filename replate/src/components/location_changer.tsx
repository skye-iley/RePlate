// location will be a context
const LocationChangerComponent = () => {
    const handleLocationChange = () => {

    }
    return(
        <button onClick={handleLocationChange}>
            Change Location
        </button>
    )
}

export default LocationChangerComponent